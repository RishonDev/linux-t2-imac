diff --git a/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c b/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c
index 2328c1aa0..9296d205b 100644
--- a/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c
+++ b/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm.c
@@ -8911,10 +8911,18 @@ static int amdgpu_dm_connector_get_modes(struct drm_connector *connector)
 	const struct drm_edid *drm_edid = amdgpu_dm_connector->drm_edid;
 	struct dc_link_settings *verified_link_cap = &dc_link->verified_link_cap;
 	const struct dc *dc = dc_link->dc;
+	int encoding = -1;
 
 	encoder = amdgpu_dm_connector_to_encoder(connector);
+	if (dc->link_srv)
+		encoding = dc->link_srv->dp_get_encoding_format(verified_link_cap);
 
 	if (!drm_edid) {
+		drm_info(connector->dev,
+			 "%s: using no-EDID fallback modes (signal=%d type=%d internal=%d aux=%d encoding=%d)\n",
+			 connector->name, dc_link->connector_signal,
+			 dc_link->type, dc_link->is_internal_display,
+			 dc_link->aux_mode, encoding);
 		amdgpu_dm_connector->num_modes =
 				drm_add_modes_noedid(connector, 640, 480);
 		if (dc->link_srv->dp_get_encoding_format(verified_link_cap) == DP_128b_132b_ENCODING)
@@ -9244,6 +9252,12 @@ static int amdgpu_dm_connector_init(struct amdgpu_display_manager *dm,
 		link,
 		link_index);
 
+	drm_info(dm->ddev,
+		 "connector init: link=%u drm_type=%d signal=%d link_type=%d internal=%d aux=%d link_id=%d\n",
+		 link_index, connector_type, link->connector_signal,
+		 link->type, link->is_internal_display, link->aux_mode,
+		 link->link_id.id);
+
 	drm_connector_attach_encoder(
 		&aconnector->base, &aencoder->base);
 
diff --git a/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_helpers.c b/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_helpers.c
index a09761f98..9fa52cfe7 100644
--- a/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_helpers.c
+++ b/drivers/gpu/drm/amd/display/amdgpu_dm/amdgpu_dm_helpers.c
@@ -1058,9 +1058,10 @@ enum dc_edid_status dm_helpers_read_local_edid(
 	} while ((edid_status == EDID_BAD_CHECKSUM || edid_status == EDID_NO_RESPONSE) && --retry > 0);
 
 	if (edid_status != EDID_OK)
-		DRM_ERROR("EDID err: %d, on connector: %s",
-				edid_status,
-				aconnector->base.name);
+		DRM_ERROR("EDID err: %d, on connector: %s (signal=%d type=%d internal=%d aux=%d)\n",
+			  edid_status, aconnector->base.name,
+			  link->connector_signal, link->type,
+			  link->is_internal_display, link->aux_mode);
 	if (link->aux_mode) {
 		union test_request test_request = {0};
 		union test_response test_response = {0};
diff --git a/drivers/gpu/drm/amd/display/dc/core/dc.c b/drivers/gpu/drm/amd/display/dc/core/dc.c
index 3e87b6a55..c30bd5855 100644
--- a/drivers/gpu/drm/amd/display/dc/core/dc.c
+++ b/drivers/gpu/drm/amd/display/dc/core/dc.c
@@ -1779,6 +1779,45 @@ static bool streams_changed(struct dc *dc,
 	return false;
 }
 
+static bool dc_log_boot_timing_failure(const struct dc *dc,
+				       const struct dc_link *link,
+				       const struct dc_sink *sink,
+				       const struct dc_crtc_timing *crtc_timing,
+				       unsigned int tg_inst,
+				       unsigned int enc_inst,
+				       const char *reason)
+{
+	DC_LOG_INFO("boot timing validation failed: link=%d signal=%d sink_signal=%d reason=%s req=%ux%u totals=%ux%u pix=%u tg=%u pref_tg=%d enc=%u\n",
+		    link->link_index, link->connector_signal, sink->sink_signal, reason,
+		    crtc_timing->h_addressable, crtc_timing->v_addressable,
+		    crtc_timing->h_total, crtc_timing->v_total,
+		    crtc_timing->pix_clk_100hz, tg_inst,
+		    link->link_enc ? link->link_enc->preferred_engine : -1, enc_inst);
+
+	return false;
+}
+
+static bool dc_log_boot_timing_mismatch(const struct dc *dc,
+					const struct dc_link *link,
+					const struct dc_sink *sink,
+					const struct dc_crtc_timing *crtc_timing,
+					unsigned int tg_inst,
+					unsigned int enc_inst,
+					const char *field,
+					unsigned int requested,
+					unsigned int hw)
+{
+	DC_LOG_INFO("boot timing validation failed: link=%d signal=%d sink_signal=%d reason=%s-mismatch req=%u hw=%u mode=%ux%u totals=%ux%u pix=%u tg=%u pref_tg=%d enc=%u\n",
+		    link->link_index, link->connector_signal, sink->sink_signal, field,
+		    requested, hw,
+		    crtc_timing->h_addressable, crtc_timing->v_addressable,
+		    crtc_timing->h_total, crtc_timing->v_total,
+		    crtc_timing->pix_clk_100hz, tg_inst,
+		    link->link_enc ? link->link_enc->preferred_engine : -1, enc_inst);
+
+	return false;
+}
+
 bool dc_validate_boot_timing(const struct dc *dc,
 				const struct dc_sink *sink,
 				struct dc_crtc_timing *crtc_timing)
@@ -1789,29 +1828,36 @@ bool dc_validate_boot_timing(const struct dc *dc,
 	struct dc_crtc_timing hw_crtc_timing = {0};
 
 	struct dc_link *link = sink->link;
-	unsigned int i, enc_inst, tg_inst = 0;
+	unsigned int i, enc_inst = 0, tg_inst = 0;
 
 	/* Support seamless boot on EDP displays only */
-	if (sink->sink_signal != SIGNAL_TYPE_EDP) {
-		return false;
-	}
+	if (sink->sink_signal != SIGNAL_TYPE_EDP)
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "non-edp-sink");
 
 	if (dc->debug.force_odm_combine) {
 		DC_LOG_DEBUG("boot timing validation failed due to force_odm_combine\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "force-odm-combine");
 	}
 
 	/* Check for enabled DIG to identify enabled display */
 	if (!link->link_enc->funcs->is_dig_enabled(link->link_enc)) {
 		DC_LOG_DEBUG("boot timing validation failed due to disabled DIG\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "disabled-dig");
 	}
 
 	enc_inst = link->link_enc->funcs->get_dig_frontend(link->link_enc);
 
 	if (enc_inst == ENGINE_ID_UNKNOWN) {
 		DC_LOG_DEBUG("boot timing validation failed due to unknown DIG engine ID\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "unknown-dig-engine");
 	}
 
 	for (i = 0; i < dc->res_pool->stream_enc_count; i++) {
@@ -1828,95 +1874,143 @@ bool dc_validate_boot_timing(const struct dc *dc,
 	// tg_inst not found
 	if (i == dc->res_pool->stream_enc_count) {
 		DC_LOG_DEBUG("boot timing validation failed due to timing generator instance not found\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "stream-encoder-not-found");
 	}
 
 	if (tg_inst >= dc->res_pool->timing_generator_count) {
 		DC_LOG_DEBUG("boot timing validation failed due to invalid timing generator count\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "invalid-timing-generator");
 	}
 
 	if (tg_inst != link->link_enc->preferred_engine) {
 		DC_LOG_DEBUG("boot timing validation failed due to non-preferred timing generator\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "non-preferred-timing-generator");
 	}
 
 	tg = dc->res_pool->timing_generators[tg_inst];
 
 	if (!tg->funcs->get_hw_timing) {
 		DC_LOG_DEBUG("boot timing validation failed due to missing get_hw_timing callback\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "missing-get-hw-timing");
 	}
 
 	if (!tg->funcs->get_hw_timing(tg, &hw_crtc_timing)) {
 		DC_LOG_DEBUG("boot timing validation failed due to failed get_hw_timing return\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "get-hw-timing-failed");
 	}
 
 	if (crtc_timing->h_total != hw_crtc_timing.h_total) {
 		DC_LOG_DEBUG("boot timing validation failed due to h_total mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "h_total",
+						  crtc_timing->h_total,
+						  hw_crtc_timing.h_total);
 	}
 
 	if (crtc_timing->h_border_left != hw_crtc_timing.h_border_left) {
 		DC_LOG_DEBUG("boot timing validation failed due to h_border_left mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "h_border_left",
+						  crtc_timing->h_border_left,
+						  hw_crtc_timing.h_border_left);
 	}
 
 	if (crtc_timing->h_addressable != hw_crtc_timing.h_addressable) {
 		DC_LOG_DEBUG("boot timing validation failed due to h_addressable mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "h_addressable",
+						  crtc_timing->h_addressable,
+						  hw_crtc_timing.h_addressable);
 	}
 
 	if (crtc_timing->h_border_right != hw_crtc_timing.h_border_right) {
 		DC_LOG_DEBUG("boot timing validation failed due to h_border_right mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "h_border_right",
+						  crtc_timing->h_border_right,
+						  hw_crtc_timing.h_border_right);
 	}
 
 	if (crtc_timing->h_front_porch != hw_crtc_timing.h_front_porch) {
 		DC_LOG_DEBUG("boot timing validation failed due to h_front_porch mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "h_front_porch",
+						  crtc_timing->h_front_porch,
+						  hw_crtc_timing.h_front_porch);
 	}
 
 	if (crtc_timing->h_sync_width != hw_crtc_timing.h_sync_width) {
 		DC_LOG_DEBUG("boot timing validation failed due to h_sync_width mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "h_sync_width",
+						  crtc_timing->h_sync_width,
+						  hw_crtc_timing.h_sync_width);
 	}
 
 	if (crtc_timing->v_total != hw_crtc_timing.v_total) {
 		DC_LOG_DEBUG("boot timing validation failed due to v_total mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "v_total",
+						  crtc_timing->v_total,
+						  hw_crtc_timing.v_total);
 	}
 
 	if (crtc_timing->v_border_top != hw_crtc_timing.v_border_top) {
 		DC_LOG_DEBUG("boot timing validation failed due to v_border_top mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "v_border_top",
+						  crtc_timing->v_border_top,
+						  hw_crtc_timing.v_border_top);
 	}
 
 	if (crtc_timing->v_addressable != hw_crtc_timing.v_addressable) {
 		DC_LOG_DEBUG("boot timing validation failed due to v_addressable mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "v_addressable",
+						  crtc_timing->v_addressable,
+						  hw_crtc_timing.v_addressable);
 	}
 
 	if (crtc_timing->v_border_bottom != hw_crtc_timing.v_border_bottom) {
 		DC_LOG_DEBUG("boot timing validation failed due to v_border_bottom mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "v_border_bottom",
+						  crtc_timing->v_border_bottom,
+						  hw_crtc_timing.v_border_bottom);
 	}
 
 	if (crtc_timing->v_front_porch != hw_crtc_timing.v_front_porch) {
 		DC_LOG_DEBUG("boot timing validation failed due to v_front_porch mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "v_front_porch",
+						  crtc_timing->v_front_porch,
+						  hw_crtc_timing.v_front_porch);
 	}
 
 	if (crtc_timing->v_sync_width != hw_crtc_timing.v_sync_width) {
 		DC_LOG_DEBUG("boot timing validation failed due to v_sync_width mismatch\n");
-		return false;
+		return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "v_sync_width",
+						  crtc_timing->v_sync_width,
+						  hw_crtc_timing.v_sync_width);
 	}
 
 	/* block DSC for now, as VBIOS does not currently support DSC timings */
 	if (crtc_timing->flags.DSC) {
 		DC_LOG_DEBUG("boot timing validation failed due to DSC\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "dsc-enabled");
 	}
 
 	if (dc_is_dp_signal(link->connector_signal)) {
@@ -1932,6 +2026,11 @@ bool dc_validate_boot_timing(const struct dc *dc,
 			tg->funcs->get_optc_source(tg,
 						&numOdmPipes, &id_src[0], &id_src[1]);
 
+		DC_LOG_INFO("boot timing validation odm state: link=%d signal=%d tg=%u enc=%u num_pipes=%u src0=%u src1=%u requested_pix=%u\n",
+			    link->link_index, link->connector_signal,
+			    tg_inst, enc_inst, numOdmPipes,
+			    id_src[0], id_src[1], crtc_timing->pix_clk_100hz);
+
 		if (numOdmPipes == 2) {
 			pix_clk_100hz *= 2;
 		} else if (numOdmPipes == 4) {
@@ -1941,7 +2040,9 @@ bool dc_validate_boot_timing(const struct dc *dc,
 
 			if (pixels_per_cycle != 1 && !dc->debug.enable_dp_dig_pixel_rate_div_policy) {
 				DC_LOG_DEBUG("boot timing validation failed due to pixels_per_cycle\n");
-				return false;
+				return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+							 tg_inst, enc_inst,
+							 "pixels-per-cycle");
 			}
 
 			pix_clk_100hz *= pixels_per_cycle;
@@ -1951,12 +2052,17 @@ bool dc_validate_boot_timing(const struct dc *dc,
 		// slightly due to rounding issues in 10 kHz units.
 		if (crtc_timing->pix_clk_100hz != pix_clk_100hz) {
 			DC_LOG_DEBUG("boot timing validation failed due to pix_clk_100hz mismatch\n");
-			return false;
+			return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "pix_clk_100hz",
+						  crtc_timing->pix_clk_100hz,
+						  pix_clk_100hz);
 		}
 
 		if (!se || !se->funcs->dp_get_pixel_format) {
 			DC_LOG_DEBUG("boot timing validation failed due to missing dp_get_pixel_format\n");
-			return false;
+			return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "missing-dp-get-pixel-format");
 		}
 
 		if (!se->funcs->dp_get_pixel_format(
@@ -1964,36 +2070,56 @@ bool dc_validate_boot_timing(const struct dc *dc,
 			&hw_crtc_timing.pixel_encoding,
 			&hw_crtc_timing.display_color_depth)) {
 			DC_LOG_DEBUG("boot timing validation failed due to dp_get_pixel_format failure\n");
-			return false;
+			return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "dp-get-pixel-format-failed");
 		}
 
 		if (hw_crtc_timing.display_color_depth != crtc_timing->display_color_depth) {
 			DC_LOG_DEBUG("boot timing validation failed due to display_color_depth mismatch\n");
-			return false;
+			return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "display_color_depth",
+						  crtc_timing->display_color_depth,
+						  hw_crtc_timing.display_color_depth);
 		}
 
 		if (hw_crtc_timing.pixel_encoding != crtc_timing->pixel_encoding) {
 			DC_LOG_DEBUG("boot timing validation failed due to pixel_encoding mismatch\n");
-			return false;
+			return dc_log_boot_timing_mismatch(dc, link, sink, crtc_timing,
+						  tg_inst, enc_inst, "pixel_encoding",
+						  crtc_timing->pixel_encoding,
+						  hw_crtc_timing.pixel_encoding);
 		}
 	}
 
 
 	if (link->dpcd_caps.dprx_feature.bits.VSC_SDP_COLORIMETRY_SUPPORTED) {
 		DC_LOG_DEBUG("boot timing validation failed due to VSC SDP colorimetry\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "vsc-sdp-colorimetry");
 	}
 
 	if (link->dpcd_caps.channel_coding_cap.bits.DP_128b_132b_SUPPORTED) {
 		DC_LOG_DEBUG("boot timing validation failed due to DP 128b/132b\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "dp-128b-132b");
 	}
 
 	if (dc->link_srv->edp_is_ilr_optimization_required(link, crtc_timing)) {
 		DC_LOG_EVENT_LINK_TRAINING("Seamless boot disabled to optimize eDP link rate\n");
-		return false;
+		return dc_log_boot_timing_failure(dc, link, sink, crtc_timing,
+						 tg_inst, enc_inst,
+						 "ilr-optimization-required");
 	}
 
+	DC_LOG_INFO("boot timing validation passed: link=%d signal=%d sink_signal=%d mode=%ux%u totals=%ux%u pix=%u tg=%u enc=%u\n",
+		    link->link_index, link->connector_signal, sink->sink_signal,
+		    crtc_timing->h_addressable, crtc_timing->v_addressable,
+		    crtc_timing->h_total, crtc_timing->v_total,
+		    crtc_timing->pix_clk_100hz, tg_inst, enc_inst);
+
 	return true;
 }
 
@@ -3464,6 +3590,12 @@ static void backup_and_set_minimal_pipe_split_policy(struct dc *dc,
 {
 	int i;
 
+	DC_LOG_INFO("%s streams=%d pipe_split=%d dynamic_odm=%d force_disable_subvp=%d\n",
+		    __func__, context->stream_count,
+		    dc->debug.pipe_split_policy,
+		    dc->debug.enable_single_display_2to1_odm_policy,
+		    dc->debug.force_disable_subvp);
+
 	if (!dc->config.is_vmin_only_asic) {
 		policy->mpc_policy = dc->debug.pipe_split_policy;
 		dc->debug.pipe_split_policy = MPC_SPLIT_AVOID;
@@ -7554,4 +7686,3 @@ bool dc_update_planes_and_stream_cleanup(
 			? update_planes_and_stream_cleanup_v3(scratch)
 			: update_planes_and_stream_cleanup_v2(scratch);
 }
-
diff --git a/drivers/gpu/drm/amd/display/dc/core/dc_resource.c b/drivers/gpu/drm/amd/display/dc/core/dc_resource.c
index 03d125f79..eae4a1088 100644
--- a/drivers/gpu/drm/amd/display/dc/core/dc_resource.c
+++ b/drivers/gpu/drm/amd/display/dc/core/dc_resource.c
@@ -3735,15 +3735,27 @@ static int acquire_resource_from_hw_enabled_state(
 	unsigned int i, inst, tg_inst = 0;
 	uint32_t numPipes = 1;
 	uint32_t id_src[4] = {0};
+	bool log_internal_boot = stream->sink &&
+		stream->sink->sink_signal == SIGNAL_TYPE_EDP;
 
 	/* Check for enabled DIG to identify enabled display */
-	if (!link->link_enc->funcs->is_dig_enabled(link->link_enc))
+	if (!link->link_enc->funcs->is_dig_enabled(link->link_enc)) {
+		if (log_internal_boot)
+			drm_info(link->ctx->logger->dev,
+				 "boot hw state unavailable: link=%d signal=%d reason=disabled-dig\n",
+				 link->link_index, link->connector_signal);
 		return -1;
+	}
 
 	inst = link->link_enc->funcs->get_dig_frontend(link->link_enc);
 
-	if (inst == ENGINE_ID_UNKNOWN)
+	if (inst == ENGINE_ID_UNKNOWN) {
+		if (log_internal_boot)
+			drm_info(link->ctx->logger->dev,
+				 "boot hw state unavailable: link=%d signal=%d reason=unknown-dig-frontend\n",
+				 link->link_index, link->connector_signal);
 		return -1;
+	}
 
 	for (i = 0; i < pool->stream_enc_count; i++) {
 		if (pool->stream_enc[i]->id == inst) {
@@ -3754,11 +3766,22 @@ static int acquire_resource_from_hw_enabled_state(
 	}
 
 	// tg_inst not found
-	if (i == pool->stream_enc_count)
+	if (i == pool->stream_enc_count) {
+		if (log_internal_boot)
+			drm_info(link->ctx->logger->dev,
+				 "boot hw state unavailable: link=%d signal=%d reason=stream-encoder-not-found enc=%u\n",
+				 link->link_index, link->connector_signal, inst);
 		return -1;
+	}
 
-	if (tg_inst >= pool->timing_generator_count)
+	if (tg_inst >= pool->timing_generator_count) {
+		if (log_internal_boot)
+			drm_info(link->ctx->logger->dev,
+				 "boot hw state unavailable: link=%d signal=%d reason=invalid-tg tg=%u tg_count=%u\n",
+				 link->link_index, link->connector_signal,
+				 tg_inst, pool->timing_generator_count);
 		return -1;
+	}
 
 	if (!res_ctx->pipe_ctx[tg_inst].stream) {
 		struct pipe_ctx *pipe_ctx = &res_ctx->pipe_ctx[tg_inst];
@@ -3777,8 +3800,14 @@ static int acquire_resource_from_hw_enabled_state(
 
 		for (i = 0; i < numPipes; i++) {
 			//Check if src id invalid
-			if (id_src[i] == 0xf)
+			if (id_src[i] == 0xf) {
+				if (log_internal_boot)
+					drm_info(link->ctx->logger->dev,
+						 "boot hw state unavailable: link=%d signal=%d reason=invalid-optc-source tg=%u src_idx=%u num_pipes=%u\n",
+						 link->link_index, link->connector_signal,
+						 tg_inst, i, numPipes);
 				return -1;
+			}
 
 			pipe_ctx = &res_ctx->pipe_ctx[id_src[i]];
 
@@ -3831,9 +3860,21 @@ static int acquire_resource_from_hw_enabled_state(
 		} else
 			stream->apply_boot_odm_mode = dm_odm_combine_mode_disabled;
 
+		if (log_internal_boot)
+			drm_info(link->ctx->logger->dev,
+				 "boot hw state acquired: link=%d signal=%d tg=%u enc=%u num_pipes=%u src0=%u src1=%u boot_odm_mode=%u preferred_engine=%d\n",
+				 link->link_index, link->connector_signal, tg_inst, inst,
+				 numPipes, id_src[0], id_src[1], stream->apply_boot_odm_mode,
+				 link->link_enc->preferred_engine);
+
 		return id_src[0];
 	}
 
+	if (log_internal_boot)
+		drm_info(link->ctx->logger->dev,
+			 "boot hw state unavailable: link=%d signal=%d reason=tg-already-occupied tg=%u\n",
+			 link->link_index, link->connector_signal, tg_inst);
+
 	return -1;
 }
 
@@ -3841,18 +3882,41 @@ static void mark_seamless_boot_stream(const struct dc  *dc,
 				      struct dc_stream_state *stream)
 {
 	struct dc_bios *dcb = dc->ctx->dc_bios;
+	bool log_internal_boot = stream->sink &&
+		stream->sink->sink_signal == SIGNAL_TYPE_EDP;
 
 	DC_LOGGER_INIT(dc->ctx->logger);
 
-	if (stream->apply_seamless_boot_optimization)
+	if (stream->apply_seamless_boot_optimization) {
+		if (log_internal_boot)
+			DC_LOG_INFO("seamless boot skip: link=%d signal=%d reason=already-marked\n",
+				    stream->link->link_index, stream->signal);
 		return;
-	if (!dc->config.allow_seamless_boot_optimization)
+	}
+	if (!dc->config.allow_seamless_boot_optimization) {
+		if (log_internal_boot)
+			DC_LOG_INFO("seamless boot skip: link=%d signal=%d reason=config-disabled\n",
+				    stream->link->link_index, stream->signal);
 		return;
-	if (dcb->funcs->is_accelerated_mode(dcb))
+	}
+	if (dcb->funcs->is_accelerated_mode(dcb)) {
+		if (log_internal_boot)
+			DC_LOG_INFO("seamless boot skip: link=%d signal=%d reason=already-accelerated\n",
+				    stream->link->link_index, stream->signal);
 		return;
+	}
 	if (dc_validate_boot_timing(dc, stream->sink, &stream->timing)) {
 		stream->apply_seamless_boot_optimization = true;
 		DC_LOG_DC("Marked stream for seamless boot optimization\n");
+		if (log_internal_boot)
+			DC_LOG_INFO("seamless boot marked: link=%d signal=%d mode=%ux%u totals=%ux%u pix=%u\n",
+				    stream->link->link_index, stream->signal,
+				    stream->timing.h_addressable, stream->timing.v_addressable,
+				    stream->timing.h_total, stream->timing.v_total,
+				    stream->timing.pix_clk_100hz);
+	} else if (log_internal_boot) {
+		DC_LOG_INFO("seamless boot skip: link=%d signal=%d reason=boot-timing-rejected\n",
+			    stream->link->link_index, stream->signal);
 	}
 }
 
diff --git a/drivers/gpu/drm/amd/display/dc/link/link_detection.c b/drivers/gpu/drm/amd/display/dc/link/link_detection.c
index b2c020071..102ce3e0d 100644
--- a/drivers/gpu/drm/amd/display/dc/link/link_detection.c
+++ b/drivers/gpu/drm/amd/display/dc/link/link_detection.c
@@ -978,6 +978,7 @@ static bool detect_link_and_local_sink(struct dc_link *link,
 	struct dc_sink *sink = NULL;
 	struct dc_sink *prev_sink = NULL;
 	struct dpcd_caps prev_dpcd_caps;
+	enum dc_connection_type pre_link_type = link->type;
 	enum dc_connection_type new_connection_type = dc_connection_none;
 	const uint32_t post_oui_delay = 30; // 30ms
 
@@ -1006,6 +1007,10 @@ static bool detect_link_and_local_sink(struct dc_link *link,
 		return false;
 	}
 
+	DC_LOG_INFO("link=%d detect start: signal=%d reason=%d pre_type=%d new_type=%d internal=%d\n",
+		    link->link_index, link->connector_signal, reason,
+		    pre_link_type, new_connection_type, link->is_internal_display);
+
 	prev_sink = link->local_sink;
 	if (prev_sink) {
 		dc_sink_retain(prev_sink);
@@ -1226,6 +1231,12 @@ static bool detect_link_and_local_sink(struct dc_link *link,
 				return true;
 			}
 
+			DC_LOG_INFO("link=%d continuing without EDID: signal=%d type=%d reason=%d sink_signal=%d internal=%d sink_count=%d\n",
+				    link->link_index, link->connector_signal,
+				    link->type, reason, sink_caps.signal,
+				    link->is_internal_display,
+				    link->dpcd_sink_count);
+
 			break;
 		default:
 			break;
diff --git a/drivers/gpu/drm/amd/display/dc/link/link_factory.c b/drivers/gpu/drm/amd/display/dc/link/link_factory.c
index 21815ad01..841c62fb7 100644
--- a/drivers/gpu/drm/amd/display/dc/link/link_factory.c
+++ b/drivers/gpu/drm/amd/display/dc/link/link_factory.c
@@ -526,7 +526,9 @@ static bool construct_phy(struct dc_link *link,
 	if (bios->funcs->get_disp_connector_caps_info) {
 		bios->funcs->get_disp_connector_caps_info(bios, link->link_id, &disp_connect_caps_info);
 		link->is_internal_display = disp_connect_caps_info.INTERNAL_DISPLAY;
-		DC_LOG_DC("BIOS object table - is_internal_display: %d", link->is_internal_display);
+		DC_LOG_INFO("link=%d bios caps: link_id=%d internal=%d\n",
+			    link->link_index, link->link_id.id,
+			    link->is_internal_display);
 	}
 
 	if (link->link_id.type != OBJECT_TYPE_CONNECTOR) {
diff --git a/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c b/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c
index aa02b38e1..74fb36b59 100644
--- a/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c
+++ b/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c
@@ -146,9 +146,20 @@ enum dp_panel_mode dp_get_panel_mode(struct dc_link *link)
 		(link->connector_signal == SIGNAL_TYPE_EDP ||
 		 (link->connector_signal == SIGNAL_TYPE_DISPLAY_PORT &&
 		  link->is_internal_display))) {
+		DC_LOG_INFO("link=%d dp_get_panel_mode: signal=%d internal=%d panel_mode_edp=%d branch=0x%x selected=%d\n",
+			    link->link_index, link->connector_signal,
+			    link->is_internal_display,
+			    link->dpcd_caps.panel_mode_edp,
+			    link->dpcd_caps.branch_dev_id, DP_PANEL_MODE_EDP);
 		return DP_PANEL_MODE_EDP;
 	}
 
+	DC_LOG_INFO("link=%d dp_get_panel_mode: signal=%d internal=%d panel_mode_edp=%d branch=0x%x selected=%d\n",
+		    link->link_index, link->connector_signal,
+		    link->is_internal_display,
+		    link->dpcd_caps.panel_mode_edp,
+		    link->dpcd_caps.branch_dev_id, DP_PANEL_MODE_DEFAULT);
+
 	return DP_PANEL_MODE_DEFAULT;
 }
 
