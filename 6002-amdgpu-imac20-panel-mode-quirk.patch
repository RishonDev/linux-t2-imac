diff --git a/drivers/gpu/drm/amd/display/dc/core/dc_resource.c b/drivers/gpu/drm/amd/display/dc/core/dc_resource.c
index eae4a10..58cf841 100644
--- a/drivers/gpu/drm/amd/display/dc/core/dc_resource.c
+++ b/drivers/gpu/drm/amd/display/dc/core/dc_resource.c
@@ -4029,6 +4029,8 @@ enum dc_status resource_map_pool_resources(
 	bool acquired = false;
 	bool is_dio_encoder = true;
 
+	DC_LOGGER_INIT(dc->ctx->logger);
+
 	calculate_phy_pix_clks(stream);
 
 	mark_seamless_boot_stream(dc, stream);
@@ -4076,6 +4078,23 @@ enum dc_status resource_map_pool_resources(
 				&pipe_ctx->link_config.dp_link_settings))
 			return DC_FAIL_DP_LINK_BANDWIDTH;
 
+		DC_LOG_INFO("resource map dp link: link=%d signal=%d internal=%d mode=%ux%u totals=%ux%u pix=%u lane_count=%d link_rate=%d spread=%d use_rate_set=%d rate_set=%d encoding=%d\n",
+			    stream->link ? stream->link->link_index : -1,
+			    stream->signal,
+			    stream->link ? stream->link->is_internal_display : false,
+			    stream->timing.h_addressable,
+			    stream->timing.v_addressable,
+			    stream->timing.h_total,
+			    stream->timing.v_total,
+			    stream->timing.pix_clk_100hz,
+			    pipe_ctx->link_config.dp_link_settings.lane_count,
+			    pipe_ctx->link_config.dp_link_settings.link_rate,
+			    pipe_ctx->link_config.dp_link_settings.link_spread,
+			    pipe_ctx->link_config.dp_link_settings.use_link_rate_set,
+			    pipe_ctx->link_config.dp_link_settings.link_rate_set,
+			    dc->link_srv->dp_get_encoding_format(
+				    &pipe_ctx->link_config.dp_link_settings));
+
 		dc->link_srv->dp_decide_tunnel_settings(stream,
 				&pipe_ctx->link_config.dp_tunnel_settings);
 
diff --git a/drivers/gpu/drm/amd/display/dc/link/link_factory.c b/drivers/gpu/drm/amd/display/dc/link/link_factory.c
index 841c62f..296757d 100644
--- a/drivers/gpu/drm/amd/display/dc/link/link_factory.c
+++ b/drivers/gpu/drm/amd/display/dc/link/link_factory.c
@@ -45,6 +45,7 @@
 #include "protocols/link_hpd.h"
 #include "gpio_service_interface.h"
 #include "atomfirmware.h"
+#include <linux/dmi.h>
 
 #define DC_LOGGER \
 	dc_ctx->logger
@@ -54,6 +55,13 @@
 	DC_LOG_HW_HOTPLUG(  \
 		__VA_ARGS__)
 
+static bool is_apple_imac20_5k(void)
+{
+	return dmi_match(DMI_SYS_VENDOR, "Apple Inc.") &&
+		(dmi_match(DMI_PRODUCT_NAME, "iMac20,1") ||
+		 dmi_match(DMI_PRODUCT_NAME, "iMac20,2"));
+}
+
 /* link factory owns the creation/destruction of link structures. */
 static void construct_link_service_factory(struct link_service *link_srv)
 {
@@ -531,6 +539,14 @@ static bool construct_phy(struct dc_link *link,
 			    link->is_internal_display);
 	}
 
+	if (is_apple_imac20_5k() &&
+	    link->link_index == 0 &&
+	    link->link_id.id == 20) {
+		link->is_internal_display = true;
+		DC_LOG_INFO("link=%d iMac20 5K quirk: forcing internal panel classification for link_id=%d\n",
+			    link->link_index, link->link_id.id);
+	}
+
 	if (link->link_id.type != OBJECT_TYPE_CONNECTOR) {
 		dm_output_to_console("%s: Invalid Connector ObjectID from Adapter Service for connector index:%d! type %d expected %d\n",
 				     __func__, init_params->connector_index,
diff --git a/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_training.c b/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_training.c
index 08e2b57..545477f 100644
--- a/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_training.c
+++ b/drivers/gpu/drm/amd/display/dc/link/protocols/link_dp_training.c
@@ -1638,6 +1638,16 @@ bool perform_link_training_with_retries(
 		(cur_link_settings.link_rate <= LINK_RATE_LOW) &&
 		(cur_link_settings.lane_count <= LANE_COUNT_ONE);
 
+	DC_LOG_INFO("link=%d training start: signal=%d panel_mode=%d attempts=%d skip_video=%d fallback=%d mode=%ux%u totals=%ux%u pix=%u rate=%d lanes=%d spread=%d encoding=%d\n",
+		    link->link_index, signal, panel_mode, attempts,
+		    skip_video_pattern, do_fallback,
+		    stream->timing.h_addressable, stream->timing.v_addressable,
+		    stream->timing.h_total, stream->timing.v_total,
+		    stream->timing.pix_clk_100hz,
+		    cur_link_settings.link_rate, cur_link_settings.lane_count,
+		    cur_link_settings.link_spread,
+		    link_dp_get_encoding_format(&cur_link_settings));
+
 	dp_trace_commit_lt_init(link);
 
 
@@ -1706,6 +1716,12 @@ bool perform_link_training_with_retries(
 			dp_trace_lt_total_count_increment(link, false);
 			dp_trace_lt_result_update(link, status, false);
 			dp_trace_set_lt_end_timestamp(link, false);
+			DC_LOG_INFO("link=%d training attempt result: attempt=%u status=%d rate=%d lanes=%d spread=%d low_bw=%d min_bw=%d\n",
+				    link->link_index, (unsigned int)j + 1,
+				    status, cur_link_settings.link_rate,
+				    cur_link_settings.lane_count,
+				    cur_link_settings.link_spread,
+				    is_link_bw_low, is_link_bw_min);
 			if (status == LINK_TRAINING_SUCCESS && !is_link_bw_low) {
 				// Update verified link settings to current one
 				// Because DPIA LT might fallback to lower link setting.
@@ -1813,4 +1829,3 @@ bool perform_link_training_with_retries(
 
 	return false;
 }
-
diff --git a/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c b/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c
index 74fb36b..79a85d1 100644
--- a/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c
+++ b/drivers/gpu/drm/amd/display/dc/link/protocols/link_edp_panel_control.c
@@ -40,6 +40,7 @@
 #include "abm.h"
 #include "resource.h"
 #include "link_dp_panel_replay.h"
+#include <linux/dmi.h>
 #define DC_LOGGER \
 	link->ctx->logger
 #define DC_LOGGER_INIT(logger)
@@ -51,6 +52,13 @@ static const uint8_t DP_VGA_LVDS_CONVERTER_ID_2[] = "sivarT";
 /* Nutmeg */
 static const uint8_t DP_VGA_LVDS_CONVERTER_ID_3[] = "dnomlA";
 
+static bool is_apple_imac20_5k(void)
+{
+	return dmi_match(DMI_SYS_VENDOR, "Apple Inc.") &&
+		(dmi_match(DMI_PRODUCT_NAME, "iMac20,1") ||
+		 dmi_match(DMI_PRODUCT_NAME, "iMac20,2"));
+}
+
 void dp_set_panel_mode(struct dc_link *link, enum dp_panel_mode panel_mode)
 {
 	union dpcd_edp_config edp_config_set;
@@ -154,6 +162,17 @@ enum dp_panel_mode dp_get_panel_mode(struct dc_link *link)
 		return DP_PANEL_MODE_EDP;
 	}
 
+	if (is_apple_imac20_5k() &&
+	    link->link_index == 0 &&
+	    link->connector_signal == SIGNAL_TYPE_EDP) {
+		DC_LOG_INFO("link=%d dp_get_panel_mode: iMac20 5K quirk forcing EDP panel mode signal=%d internal=%d panel_mode_edp=%d branch=0x%x selected=%d\n",
+			    link->link_index, link->connector_signal,
+			    link->is_internal_display,
+			    link->dpcd_caps.panel_mode_edp,
+			    link->dpcd_caps.branch_dev_id, DP_PANEL_MODE_EDP);
+		return DP_PANEL_MODE_EDP;
+	}
+
 	DC_LOG_INFO("link=%d dp_get_panel_mode: signal=%d internal=%d panel_mode_edp=%d branch=0x%x selected=%d\n",
 		    link->link_index, link->connector_signal,
 		    link->is_internal_display,
